{
  "name": "start-workshop",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "deploy:prod": "sls deploy --stage prod",
    "deploy:dev": "sls deploy --stage dev",
    "deploy:local": "sls offline start --stage local"
  },
  "author": "Eugene",
  "license": "ISC",
  "devDependencies": {
    "serverless-offline": "^8.2.0"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "serverless-env-generator": "^1.4.1",
    "serverless-prune-plugin": "^1.6.1"
  }
}
